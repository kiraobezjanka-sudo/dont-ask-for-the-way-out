{
  "name": "dont-ask-for-the-way-out",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/game-data.js && node --check src/game-model.js && node --check src/app.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs"
  }
}

